import React from 'react';
import PropTypes from 'prop-types';

function Notepad(props) {
  return (
    <div>
      <h1>Notepad</h1>
    </div>
  );
}

Notepad.propTypes = {};

export default Notepad;
